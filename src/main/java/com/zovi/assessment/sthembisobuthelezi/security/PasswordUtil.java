package com.zovi.assessment.sthembisobuthelezi.security;

import org.springframework.stereotype.Component;

@Component
public class PasswordUtil {
//    public String encode(String rawPassword) {
//        String encodedString = URLEncoder.encode(rawPassword, StandardCharsets.UTF_8);
//        return BCrypt.hashpw(encodedString, BCrypt.gensalt(4));
//    }
//
//    public boolean matches(String rawPassword, String encodedPassword) {
//        String encodedString = URLEncoder.encode(rawPassword, StandardCharsets.UTF_8);
//        return BCrypt.checkpw(encodedString, encodedPassword);
//    }
}
